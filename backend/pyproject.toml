[project]
name = "adversarial-sandbox"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.110",
    "uvicorn[standard]>=0.29",
    "pydantic>=2.6",
    "scikit-learn>=1.4",
    "numpy>=1.26",
    "matplotlib>=3.8",
    "torch>=2.2",
    "transformers>=4.44",
    "huggingface-hub>=0.24",
]

[project.optional-dependencies]
dev = ["pytest>=8.0", "httpx>=0.27"]
# Tooling for regenerating the README media (scripts/screenshots.py, scripts/hero_gif.py).
# Playwright drives the local Chrome via channel="chrome" — no browser download needed.
screenshots = ["playwright>=1.45", "pillow>=10.0"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["."]
include = ["adversarial_sandbox*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
